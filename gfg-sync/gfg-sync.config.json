{
  "_comment": "Open a GFG problem page, DevTools > Network > filter XHR/Fetch. Find the request that returns the problem JSON (title, statement, examples). Copy its URL pattern here, replacing the slug with {slug}.",
  "apiEndpointTemplate": "",
  "outputDir": "./docs",
  "solutionsDir": "./solutions"
}
